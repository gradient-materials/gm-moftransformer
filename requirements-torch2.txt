# Install PyTorch with CUDA 12.4 first, then the package:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-torch2.txt
#   pip install -e .
-r requirements.txt
